# Notices

okkskin 是**非官方**的 Codex 桌面端换肤工具,**与 OpenAI 无隶属、未获其背书或赞助**。

## 致谢
okkskin 的思路最初受 **Codex-Dream-Skin(MIT)** 启发。实现为原创、机制不同:
采用「inspector 脉冲」(SIGUSR1 / `process._debugProcess` 短暂打开主进程 Node inspector →
经 `webContents.executeJavaScript` + `dom-ready` 钩子注入 → `process._debugEnd` 立即关闭端口),
配色靠覆盖 Codex 自身的设计 token —— 与 Codex-Dream-Skin 的 `--remote-debugging-port` + 选择器方案不同。

## 许可
`LICENSE`(MIT)适用于本仓库源码。不授予:OpenAI/Codex 商标、产品名、logo;官方应用二进制/`.app`/`app.asar`;用户自备图片或第三方美术素材。

## 运行时
不分发 Node.js;运行时使用用户官方 Codex 应用内已签名的 Node 运行时。

## 安全模型
换肤通过「脉冲」完成:本地 Node inspector 仅在注入那 ~1 秒于 `127.0.0.1` 打开,注完即 `process._debugEnd` 关闭,**不在整会话期间保持开放**。即便如此,仅建议在**个人可信设备**使用。`okkskin disable` / `restore` 可完全移除。
